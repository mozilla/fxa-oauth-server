--  Drop `publicClient` column from the `clients` table.

-- ALTER TABLE publicClient DROP COLUMN publicClient,
-- ALGORITHM = INPLACE, LOCK = NONE;

-- ALTER TABLE publicClient DROP COLUMN publicClient,
-- ALGORITHM = INPLACE, LOCK = NONE;

-- UPDATE dbMetadata SET value = '17' WHERE name = 'schema-patch-level';
